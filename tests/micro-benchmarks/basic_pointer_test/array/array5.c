int main(){

	int* pins_per_class;
	int a[100];
	int *b[100];
	unsigned int i;
	pins_per_class = (int*) malloc(100);

	if(10){
	//	for(i = 0; i < 10; i++)
		pins_per_class[1] = i;
		a[10] = 200;
		*b[20] = 300;
	}

	free(pins_per_class);

	printf("%d",a[10]);

}
